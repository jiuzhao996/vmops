---
title: "系统测试与验证"
description: "基于KVM虚拟化的轻量级私有云管理平台 - 系统测试与验证"
tags: [系统测试, 功能测试, KVM演示, 性能, 毕设]
---

# 系统测试与验证

> 对应论文章节：第六章 系统测试。
> 素材来源：旧《开发进度》测试记录、DB 审计数据、virsh / qemu-img 环境实测、`go test -race` 输出、`docker build` 输出、服务端日志。

[TOC]

------

## 1. 测试环境与工具

- 硬件：本机即 KVM 宿主机（12 线程、16GB 物理内存）。
- 软件：Ubuntu / Go / MySQL8(Docker) / libvirt / Vue3 构建产物。
- 测试账号（admin/password、user/123456，user 为 viewer 只读角色）。
- 测试层次与工具：

| 层次 | 工具 | 范围 |
|---|---|---|
| 单元测试 | `go test -race ./...` | 共 138 个顶层测试函数 / 约 950 个子用例 / 7 个包，纯函数目标覆盖率基本 100%（详见第 4 章） |
| 接口/端到端回归 | `scripts/smoke.sh`（curl + python3 断言） | 只读接口 + `/metrics` + 创建/删除 task 全链路 + 硬件管理，23 项断言 |
| 安全验证 | curl 构造载荷、python3 手工签发伪造 JWT、`wscat` 驱动 WebSocket 首帧 | SQL 注入面、XML 注入面、RBAC 边界、算法混淆、命令行选项注入、SSH 目标白名单 |
| 虚拟化语义验证 | `qemu-img info` / `qemu-img check` / `virsh vol-dumpxml` / `virsh dumpxml` | 增量克隆是否真有 backing file、克隆机 MAC 是否唯一、删机后共享卷是否保留（详见第 9 章） |
| 性能对比实验 | 任务路径毫秒计时、`qemu-img info` / `du` 产物占用、`ps` RSS 采样、启动日志时间戳 | linked clone vs 全量拷贝、平台资源占用、冷启动耗时（详见第 5 章） |
| 静态检查 | `go build ./...`、`go vet ./...`、`gofmt -l .` | 全仓库（`.golangci.yml` 已就位并对齐 `go: "1.25"`，但本机未安装 `golangci-lint`，本轮未执行） |
| 容器构建验证 | `docker build --no-cache` | Dockerfile 重写后的可构建性（结果见第 7 章） |
| 人工功能测试 | 浏览器 + `virsh` 对照 | 各业务模块 |

## 2. 功能测试

### 2.1 认证与用户管理

- [x] 登录成功/失败、token 过期、角色权限。
- [x] 签名算法锁定：伪造 `alg=none` 与 `alg=HS512` 的 token 均被拒（详见 6.4）。

### 2.2 宿主机管理

- [x] CRUD、连通性测试、状态采集（hostname/kernel/mem）。
- [x] `ssh_ip` 格式校验：以连字符开头等异常取值被拒（详见 6.6）。

### 2.3 虚拟机生命周期

- [x] 创建（真实建机，存储池建卷 + DomainDefineXML）→ 启动 → 停止 → 重启 → 删除 全流程。
- [x] 状态实时同步验证（virsh 对照）。
- [x] 带 ISO 光驱创建（Rocky-10.2 引导至安装界面）。
- [x] 停止策略：优雅关机 + 15s 轮询超时强制断电（现经异步 `stop_vm` 任务执行，实测通过）。
- [x] 存量 VM 导入/纳管：扫描出宿主机 15 台 virsh 既有域（CPU/内存/MAC/磁盘解析正确）→ 勾选导入 DB → rescan 全部变为已纳管，仪表盘 vm_count 由 0 升至 15。
- [x] IP 自动回填（DHCP + QGA 双通道）：列表/详情请求惰性触发（全局 30s 节流），DHCP 租约按 MAC 命中回填（对应 `virsh net-dhcp-leases`）；租约未命中且客户机已装 qemu-guest-agent 时改走 QGA（对应 `virsh domifaddr --source agent`，静态 IP 不进租约时的唯一自动途径）。
- [x] 非法 ID 提前拒绝：`/api/vms/<非数字>` 返回 400 而非落到 404（详见 6.1）。

### 2.4 存储池管理

- [x] 池列表（容量/路径/状态）、卷创建/删除。
- [x] 新建目录型存储池、删除（destroy + undefine）。
- [x] 建池路径与建卷格式校验：非规范路径、白名单外格式被拒（详见 6.5）。

### 2.5 网络管理

- [x] 网络列表（网桥/转发/网关）、启停、删除（运行中先 destroy 再 undefine）。
- [x] 创建 NAT 网络（模板）、XML 定义。
- [x] 网关 IPv4 校验：非法网关被拒（详见 6.5）。

### 2.6 快照管理

- [x] 快照创建/列表/删除/回滚；快照创建支持描述字段，列表返回描述与创建时间，实测通过。

### 2.7 网页控制台

- [x] noVNC token 解析链路（websockify JSONTokenApi → VNC 5900）。
- [x] WebSocket 升级 101 验证。
- [x] 三链路会话埋点：VNC/SSH/串口连接均在审计中心「控制台会话」tab 可见，SSH/串口可强制断开，VNC 靠过期收敛，实测通过。
- [x] 按角色分入口：admin 三入口齐全；viewer 只见图形控制台且为只读观看（详见 2.20、6.2、6.3）。
- [x] Web 终端目标白名单：公网、环回、云元数据、主机名等目标被拒，私有网段放行至真实拨号（详见 6.7）。

### 2.8 XML 编辑

- [x] dumpxml 查看、校验后 define 更新（vcpu 修改对照 virsh 验证）。

### 2.9 审计日志

- [x] 中间件自动记录（覆盖 login/vm/host/image/storage/network）、多条件筛选、分布统计。

### 2.10 仪表盘

- [x] 总览统计（含存储池/网络计数）、状态分布正确性。

### 2.11 异步任务系统

- [x] 任务创建轮询成功：提交创建/克隆/停止后返回 202，前端轮询至成功并回填 VM，实测通过。
- [x] 删除清理：仅终态任务可删除，已完成任务可批量清理，实测通过。
- [x] 任务详情抽屉：result 解析展示 vm_id（跳详情）与 kept_volumes（删卷保护原因），真分页 total 为 Count 真值，实测通过。

### 2.12 监控中心（Alertmanager + Grafana 融合）

- [x] 告警代理：`GET /api/monitor/alerts` 转发 Alertmanager，实测返回真实 firing 告警；AM 不可达返回 502 并页内提示启动方法。
- [x] Grafana kiosk 嵌入：容器化 Grafana 13.2.1 经匿名只读 + allow_embedding 嵌入产品页，双 provisioned 看板（宿主机 5 面板 / 虚拟机 6 面板）页内单选切换、均有实时数据；混合形态勿启动 websockify 容器（抢 6080）。
- [x] Grafana 探活兜底：进入页面经 `GET /api/monitor/grafana-status` 后端探测，容器未启动时亮「Grafana 未连接 + 重试」提示层（浏览器侧 onerror/load/超时对错误页 iframe 均判不了，必须后端代探）。
- [x] 告警历史：`POST /api/monitor/webhook`（可选 token 鉴权）按 fingerprint 去重入库，`GET /api/monitor/alerts/history` 分页追溯，监控中心「告警历史」卡片可见。
- [x] file_sd 目标预览卡：`GET /api/monitor/file-sd` 返回 `{enabled, items}`，卡片区分「服务发现已启用 / FILE_SD_PATH 未配置」两种空态。
- [x] 仪表盘告警概览卡：firing 数量与最近告警联动，有待处理告警时红边 +「N 条待处理」徽标，可切到监控 tab。

### 2.13 历史性能曲线（Prometheus 当历史库）

- [x] 仪表盘主机大盘：进页面即画满 60 分钟历史（query_range 预填 60 点），刷新不再清零，3s 轮询在尾部无缝追加。
- [x] 虚拟机列表迷你曲线：`vm-history` 批量接口一次预填全部运行中 VM 的卡片曲线。
- [x] 虚拟机详情大图：`stats-history` 预填（实测 node1 覆盖 12 分钟运行区间，含开机 CPU 尖峰）；关机时段无采样属正确行为。
- [x] 边界：内存占比 0/0 产生 NaN 时接口仍返回合法 JSON（服务端跳过非有限点），VM 不存在返回 404。

### 2.14 审计中心与会话管理增厚

- [x] 审计中心双 tab：操作日志（admin）+ 控制台会话（全角色），viewer 不发起 admin 接口请求。
- [x] 会话筛选与真分页：type/vm_name/username 过滤 + page/page_size，last_seen 列可视。

### 2.15 个人中心与用户管理

- [x] 个人中心：资料只读展示、自助改密（旧密码校验/≥6 位/成功后强制重登）、轮询偏好保存生效。
- [x] 用户管理：创建（角色白名单/密码≥6 位）、编辑、禁用、不能删除自己；非法角色返回 400。

### 2.16 系统设置做实

- [x] 可写运行参数：默认存储池/VNC token 有效期/VNC 会话过期时长写库即生效，无需重启。
- [x] 校验：非法取值（如 token 有效期 999）返回 400 逐字文案；消费点（建机默认池/告警 TTL/会话清扫）实时读取。

### 2.17 存储安全与孤儿卷清理

- [x] 删卷守卫：删除被挂载/被镜像库引用/是 backing 父盘的卷返回 409 中文原因；卷管理弹窗「在用」徽标与确认文案联动。
- [x] 孤儿卷清理：建零引用测试卷 → 提交 cleanup_volumes 任务 → 精准删除该卷、有引用卷全部保留（kept 含原因），实测通过。

### 2.18 登录限流（安全）

- [x] 同 IP 连续 5 次失败后第 6 次返回 429「登录失败次数过多」，锁定期间正确密码同样拒绝；成功登录清零计数；不同 IP 互不影响。

### 2.19 会话管理

- [x] 会话创建断开：三链路建连即有记录，SSH/串口强制断开后浏览器掉线且记录关闭，实测通过。

### 2.20 权限边界

- [x] viewer 只读边界：viewer 的 GET 与图形控制台观看通过，变更操作返回 403；`/users`、`/audit`、`/settings` 仅 admin 可见，实测通过。
- [x] viewer 的客户机写入通道边界：`GET /api/vms/:id/terminal` 与 `GET /api/vms/:id/serial` 对 viewer 返回 403（两者虽为 GET，但建立的是对客户机的双向写入通道），admin 正常放行，实测通过。
- [x] 图形控制台只读下发：`POST /api/vms/:id/vnc-token` 响应的 `view_only` 按角色正确取值（admin=false / viewer=true），前端以 noVNC 只读模式打开并显示只读标签，实测通过。
- [x] 前端入口收敛：viewer 的控制台页不渲染 SSH / 串口入口，选择页出现只读说明条，实测通过。

### 2.21 增量克隆与删除保护

- [x] 增量克隆真实性：克隆子卷 `qemu-img info` 有 `backing file:` 行指向源盘，`virsh vol-dumpxml` 有 `<backingStore>` 节点，`qemu-img check` 报 `No errors were found on the image`，实测通过（**修复前前两项均不存在，见 10.2 F-19**）。
- [x] 镜像克隆建机：基于模板建机成功，子卷初始占用远小于父卷虚拟容量（写时复制生效），实测通过。
- [x] 克隆机身份唯一性：源机 MAC `52:54:00:7a:ad:94` → 克隆机 `52:54:00:6d:f2:b1`，UUID 亦不同，DB 记录与 libvirt 侧一致，实测通过（**修复前 MAC 与源机完全相同**）。
- [x] 删除保护：删源机后源盘被保留、`kept_volumes` 给出精确原因、克隆机磁盘 `qemu-img check` 无错误；镜像库登记的文件在引用它的 VM 被删后仍存在并标注「是镜像库登记的共享基镜像」，实测通过。
- 上述后三项即本批的 3 组端到端用例（全部 PASS），编号见 3 章 T-Clone-02～04。

## 3. 新增功能测试记录表

| 编号 | 功能 | 输入/操作 | 预期 | 实际结论 |
|---|---|---|---|---|
| T-Task-01 | 任务创建轮询成功 | 提交创建 VM，轮询 `GET /api/tasks/:id` | 状态流转 pending→running→success，进度递增，结果含 vm_id | 实测通过 |
| T-Task-02 | 任务失败可观测 | 提交非法参数任务 | 置 failed 并返回中文 error | 实测通过 |
| T-Task-03 | 删除清理 | 删除终态任务 / 清理已完成 | 仅 success/failed 可删，进行中拒绝 | 实测通过 |
| T-Sess-01 | 会话创建 | VNC/SSH/串口分别建连 | 审计中心「控制台会话」tab 出现对应记录 | 实测通过 |
| T-Sess-02 | 强制断开 | 断开 SSH/串口会话 | 浏览器掉线，记录置 closed | 实测通过 |
| T-Sess-03 | VNC 不可强断 | 尝试断开 VNC 会话 | 返回明确提示，引导关闭页签 | 实测通过 |
| T-RBAC-01 | viewer 读边界 | viewer 访问各列表与详情 | 全部 200 | 实测通过 |
| T-RBAC-02 | viewer 写边界 | viewer 提交创建/删除/改配 | 一律 403 | 实测通过 |
| T-RBAC-03 | viewer 图形控制台例外 | viewer 申请 VNC token | 200，`view_only=true`，可查看不可操作 | 实测通过 |
| T-RBAC-04 | viewer 客户机写入通道 | viewer 请求 `/terminal`、`/serial` | 403 + 中文原因 | 实测通过 |
| T-Snap-01 | 快照带描述 | 创建快照填写描述 | 列表展示描述与创建时间 | 实测通过 |
| T-Clone-01 | 增量克隆真实性 | 克隆一台 VM 后对子盘执行 `qemu-img info` / `virsh vol-dumpxml` / `qemu-img check` | 有 `backing file:` 行、有 `<backingStore>` 节点、镜像结构无错误 | 实测通过（修复前无 backing file，见 10.2 F-19） |
| T-Clone-02 | 克隆机 MAC 唯一 | 克隆一台带网卡的 VM，比对源机与克隆机的 MAC（DB 记录 + `virsh dumpxml`） | 两者 MAC 不同，格式为 `52:54:00:xx:xx:xx`，DB 与 libvirt 一致 | 实测通过（源 `52:54:00:7a:ad:94` → 克隆 `52:54:00:6d:f2:b1`） |
| T-Clone-03 | 删源机不伤克隆机 | 确认子盘 backing 指向源盘后，删除源虚拟机 | 源盘被保留，`kept_volumes` 给出「是增量克隆父盘，仍被 N 个子卷依赖」，克隆机磁盘 `qemu-img check` 无错误 | 实测通过 |
| T-Clone-04 | 共享基镜像不被误删 | 删除一台以 `source_image_id` 直接引用云镜像创建的 VM | 镜像文件仍存在，`kept_volumes` 标注「是镜像库登记的共享基镜像」，`images` 表记录与文件仍对应 | 实测通过 |

viewer 读写边界矩阵（200/403）：

| 角色 | GET 各资源 | POST/PUT/DELETE 各资源 | POST vnc-token | GET terminal / serial | /users、/audit、/settings |
|---|---|---|---|---|---|
| admin | 200 | 200 | 200（`view_only=false`） | 建立 WebSocket | 200 |
| viewer | 200 | 403 | 200（`view_only=true`） | **403** | 403 |

## 4. 单元测试（`go test -race`）

项目此前无任何 `*_test.go`。测试建设按「先补最容易造成不可恢复后果的路径」推进，分批落地，
当前共 **138 个顶层测试函数 / 约 950 个子用例 / 7 个包**，`go test -race ./...` 全部 PASS：

| 包 | 文件 | 函数数 | 被测对象 | 补齐批次 |
|---|---|---|---|---|
| `service/console` | `conn_test.go` | 5 | WebSocket 写入串行化（并发写 panic 会带走整进程） | P1 |
| `service/virt` | `clone_test.go` | 8 | 克隆配置派生的纯函数（MAC/UUID 唯一性、深拷贝、系统盘定位） | P2 |
| `service/virt` | `spec_test.go` | 17 | domain XML 编解码（解析/生成/转义/往返/target 分配/头寸计算/设备细节） | P4+ |
| `service/virt` | `cloudinit_test.go` | 6 | seed ISO 生成（卷标/内含文件/字段注入） | P4 |
| `service/virt` | `storage_test.go` | 7 | 卷/池 XML 生成与转义 | P4 |
| `service/virt` | `network_test.go` | 4 | 网络 XML 生成与转义（含 DHCP 边界） | P4 |
| `service/virt` | `state_test.go` | 4 | libvirt 状态映射（四值收敛） | P4 |
| `service/virt` | `snapshot_test.go` | 4 | `xmlEscape` 与快照状态映射 | P4 |
| `service/virt` | `device_test.go` | 1 | 设备存在性解析（guest-agent 通道/rng 幂等补齐） | 监控闭环批 |
| `handler` | `param_test.go` | 4 | 路径参数主键解析（SQL 注入面的回归锚点） | P4 |
| `handler` | `response_test.go` | 7 | 友好错误提取与响应体脱敏 | P4 |
| `handler` | `terminal_test.go` | 4 | SSH 目标白名单（67 例含端口边界与 `itoa` 差分） | P4 |
| `handler` | `validate_test.go` | 12 | 存储/网络/宿主机/VM 参数校验（186 例） | P4 |
| `handler` | `auth_test.go` | 2 | 登录失败限流（窗口/阈值/成功清零） | 监控闭环批 |
| `handler` | `history_test.go` | 2 | PromQL label 转义与时间窗钳制 | UX 批 |
| `handler` | `storage_refs_test.go` | 2 | 卷引用计算与 409 守卫文案 | UX 批 |
| `handler` | `alert_webhook_test.go` | 4 | 告警网关（fingerprint 去重 upsert/恒 200） | 监控闭环批 |
| `handler` | `vm_detach_test.go` | 1 | 分离磁盘删卷守卫判定 | 设备批次 |
| `middleware` | `jwt_test.go` | 16 | 密码哈希、Token 往返、算法混淆、两道权限中间件 | P4 |
| `service/tasks` | `manager_test.go` | 11 | panic 兜底、有界入队、友好错误 | P4 |
| `service/tasks` | `vm_tasks_test.go` | 14 | payload 解析、名称校验、MAC/UUID 生成 | P4 |
| `service/monitor` | `file_sd_test.go` | 4 | file_sd 目标生成（去重/原子写） | 监控闭环批 |
| `service/setting` | `setting_test.go` | 2 | 设置键白名单与取值校验 | UX 批 |

两批的可测性来源不同，值得对照：`console.Conn` 本身不依赖外部服务，用 `httptest` 起回环 WebSocket
即可测；`service/virt` 绝大多数函数都要 libvirt 连接、无法直接单测，因此 P2 修复克隆缺陷时**刻意把
配置派生逻辑抽成不触碰 libvirt 的纯函数**（`buildCloneSpec` / `systemDiskIndex` / `randomMACAddr`），
使核心正确性得以被断言。这也是「为可测性而重构」的一个具体实例。

### 4.1 WebSocket 写入串行化（`service/console/conn_test.go`，5 例）

测试用 `httptest.NewServer` 起回环 WebSocket 服务端，服务端侧以被测的 `console.Conn` 包装，
客户端侧持续读取以避免写缓冲打满。

| 编号 | 用例（测试函数） | 输入/操作 | 预期 | 实际 |
|---|---|---|---|---|
| U-Conn-01 | `TestConnConcurrentWrite` | 8 个协程 × 每协程 50 帧，二进制帧与 JSON 帧交替（覆盖 `WriteMessage` 与 `WriteJSON` 两条写路径） | 无 `concurrent write to websocket connection` panic，无写错误，`-race` 无告警 | PASS |
| U-Conn-02 | `TestConnWriteAfterClose` | 先 `Close()`，再 `WriteMessage` 与 `WriteJSON` | 两者均返回 `ErrConnClosed`（桥接协程据此退出转发循环） | PASS |
| U-Conn-03 | `TestConnCloseIdempotent` | `CloseWithReason("管理员已断开连接")` 后再 `Close()`、再 `CloseWithReason()` | 后续关闭均返回 `nil`（handler 的 `defer Close` 与管理员强断会双重触发） | PASS |
| U-Conn-04 | `TestConnCloseWithReasonDeliversFrame` | 服务端 `CloseWithReason("管理员已断开连接")` | 客户端读到 `*websocket.CloseError`，`Code == CloseNormalClosure`，`Text` 为该中文原因 | PASS |
| U-Conn-05 | `TestConnCloseUnblocksReader` | 读协程阻塞在 `ReadMessage()` 后关闭连接 | `ReadMessage` 在 3 秒内返回错误（管理员强制断开的收敛依赖此行为） | PASS |

执行结果：

```
$ go test -race -v ./service/console/
=== RUN   TestConnConcurrentWrite
--- PASS: TestConnConcurrentWrite (0.01s)
=== RUN   TestConnWriteAfterClose
--- PASS: TestConnWriteAfterClose (0.00s)
=== RUN   TestConnCloseIdempotent
--- PASS: TestConnCloseIdempotent (0.00s)
=== RUN   TestConnCloseWithReasonDeliversFrame
--- PASS: TestConnCloseWithReasonDeliversFrame (0.00s)
=== RUN   TestConnCloseUnblocksReader
--- PASS: TestConnCloseUnblocksReader (0.05s)
PASS
ok      github.com/jiuzhao/vmops/service/console        1.084s
```

### 4.2 克隆配置派生（`service/virt/clone_test.go`，8 例）

测试夹具 `newSourceSpec()` 构造一台「首位是 cdrom + 两块磁盘 + 两块网卡」的源虚拟机配置，
用以同时覆盖系统盘定位、磁盘深拷贝与多网卡 MAC 三条路径。全部为纯函数测试，不需要 libvirt 连接。

| 编号 | 用例（测试函数） | 输入/操作 | 预期 | 实际 |
|---|---|---|---|---|
| U-Clone-01 | `TestRandomMACAddrFormatAndUniqueness` | 连续生成 500 个 MAC | 全部匹配 `^52:54:00:[0-9a-f]{2}(:[0-9a-f]{2}){2}$`，且无重复 | PASS |
| U-Clone-02 | `TestRandomUUIDV4Format` | 连续生成 100 个 UUID | 符合 RFC 4122 v4（第三段以 `4` 开头、第四段首字符落 `8/9/a/b`） | PASS |
| U-Clone-03 | `TestSystemDiskIndexSkipsCDROM` | 首位为 cdrom 的磁盘列表；仅含 cdrom；无磁盘 | 依次返回 `1`、`-1`、`-1`（光驱不能作 linked clone 父盘） | PASS |
| U-Clone-04 | `TestBuildCloneSpecRegeneratesAllMACs` | 对两块网卡的源 spec 派生克隆 spec | **两块网卡的 MAC 都与源机不同、彼此不同、格式合法**；MAC 之外的网卡属性原样保留；源 spec 不被污染 | PASS |
| U-Clone-05 | `TestBuildCloneSpecDeepCopiesDisks` | 同上，检查磁盘切片 | 只有系统盘的 `Source`/`BackingFile` 被改写，cdrom 与数据盘不动，源机磁盘不被污染 | PASS |
| U-Clone-06 | `TestBuildCloneSpecIdentityFields` | 同上，检查身份字段 | 名称与 UUID 已更新且 UUID 格式合法、`RawXML` 清空、硬件规格（vcpu/内存）沿用源机、源机身份字段不受影响 | PASS |
| U-Clone-07 | `TestBuildCloneSpecNoInterfaces` | 无网卡的纯串口机 | 不报错，克隆机同样无网卡 | PASS |
| U-Clone-08 | `TestCloneSpecProducesValidXML` | 派生后交 `BuildDomainXML` 生成真正写给 libvirt 的 XML | 输出**不含**源机两个 MAC、**含**新 MAC、含新名称 | PASS |

U-Clone-04 是本文件的核心用例，直接对应修复前的缺陷：`spec := *source` 只复制切片头，
`Interfaces` 未深拷贝也未换 MAC，克隆机与源机 MAC 完全相同，两台同时开机即同网段地址冲突。
U-Clone-08 则把断言推到「真正交给 libvirt 的那一步」，避免只测中间结构体而漏掉生成环节。

执行结果：

```
$ go test -race -v ./service/virt/
=== RUN   TestRandomMACAddrFormatAndUniqueness
--- PASS: TestRandomMACAddrFormatAndUniqueness (0.00s)
=== RUN   TestRandomUUIDV4Format
--- PASS: TestRandomUUIDV4Format (0.00s)
=== RUN   TestSystemDiskIndexSkipsCDROM
--- PASS: TestSystemDiskIndexSkipsCDROM (0.00s)
=== RUN   TestBuildCloneSpecRegeneratesAllMACs
--- PASS: TestBuildCloneSpecRegeneratesAllMACs (0.00s)
=== RUN   TestBuildCloneSpecDeepCopiesDisks
--- PASS: TestBuildCloneSpecDeepCopiesDisks (0.00s)
=== RUN   TestBuildCloneSpecIdentityFields
--- PASS: TestBuildCloneSpecIdentityFields (0.00s)
=== RUN   TestBuildCloneSpecNoInterfaces
--- PASS: TestBuildCloneSpecNoInterfaces (0.00s)
=== RUN   TestCloneSpecProducesValidXML
--- PASS: TestCloneSpecProducesValidXML (0.00s)
PASS
ok      github.com/jiuzhao/vmops/service/virt   1.019s
```

### 4.3 第三批：安全校验与 XML 编解码（P4，15 个文件）

P4 批次把 P0–P2 的安全加固固化为回归测试，并补齐 `service/virt` 的 XML 编解码覆盖。
核心用例选录（全部为表驱动 + 中文场景名，`go test -race` 全绿）：

**`service/virt/spec_test.go`（13 函数，domain XML 是全平台配置流转的枢纽）**

| 用例 | 风险点 |
|---|---|
| 最小域/复杂域逐字段解析 | 纳管、克隆、删除都读同一入口，字段错位会连带污染 |
| 内存单位换算（KiB/MiB/GiB/bytes/无单位/大小写/空白） | 算错直接影响创建向导的内存展示 |
| vcpu `current` 优先、缺失回退 chardata | 热升级头寸存于 chardata，取错会把上限当当前核数 |
| 生成器参数校验（nil/空名/0 核/负内存） | 非法定义喂给 libvirt define |
| **XML 转义专项**：名称/路径/网络名塞入闭合载荷，反解确认值原样、节点数无增加 | P1「字符串拼接改 marshal」的回归锚点 |
| **往返测试** `Parse(Build(spec))` | 抓编解码不对称（如某字段只写不读，克隆会静默丢配置） |
| `NextDiskTarget`（空盘/占位/空洞/跨 bus/26 块续编） | target 冲突会导致热插拔失败 |

往返测试中显式断言了两处「设计使然的不对称」：`RawXML`/`Autostart` 不在 domain XML 中（还原为空），
光驱空 driver 生成时按 `raw` 落盘（反解回来是 `raw` 而非空）。把「已知的不对称」写进测试，
可防止后人误判为 bug 而「修复」。

测试中另发现一处真实缺陷（已记录未修）：`diskSuffixIndex("aa")` 算出 0，与 `"a"` 冲突——
该函数是无偏置 26 进制，26 块磁盘占满后续编的 `"vdaa"` 在第 28 块时会重复。26 块以上磁盘的
虚拟机现实中几乎不存在，故断言现状 + TODO 注释，不在此轮修复。

**`handler/terminal_test.go`（4 函数 / 67 例）**：`validateSSHTarget` 的双分支（VM 已记录 IP 则精确匹配；
无记录 IP 则仅 RFC1918 私有网段字面量）+ 端口边界 + `itoa` 差分对照。每条拒绝断言错误信息含汉字、
不含 `panic/goroutine/libvirt/gorm/sql` 等内部词。

**`middleware/jwt_test.go`（16 函数 / 115 例）**：含算法混淆四条——手工 HS256 合法 token 控制组、
`alg=none`、`alg=HS512`、`alg=HS384`，其中 HS512 一条带反证（去掉 `WithValidMethods` 后同一 token
被判合法，证明该行代码在承压）；`OperatorMiddleware` 走真实路由注册测（裸 `CreateTestContext`
的 `FullPath()` 恒为空，会把后缀判定测出假阳性）。

**`service/tasks/manager_test.go`（11 函数）**：panic 兜底覆盖 6 种真实 panic（nil map 写入、
nil 解引用、越界、除零、显式 panic、panic 值含口令与路径），断言 panic 原文不进错误值；
入队超时用例不接数据库（`DB` 为 nil 让 `markFailed` 内部 panic 并自行 recover），顺带证明
「DB 再出意外也不带走 worker」。

**`handler/validate_test.go`（12 函数 / 186 例）**：`validPoolPath` 含 XML 闭合载荷、
`validateSSHIP` 含 `-f`/`--flood` 与 253/254 长度边界、`sanitizeFileName` 附加「输出永不含 `/`」不变式。

目标函数级覆盖率（包级数字因分母含需 DB+libvirt 的 HTTP 处理器而偏低，无意义）：
`handler/param.go` 100%、`handler/response.go` 100%、`ParseDomainXML` 95.7%、
`BuildDomainXML` 97.5%、`StateToPlatform` 100%、`xmlEscape` 100%、
`validateSSHTarget`/`validPoolPath`/`validIPv4`/`validateSSHIP` 等校验函数 100%。

### 4.4 反证实验（证明修复有效而非纸面工程）

单元测试全部通过只能说明修复后的代码没问题，不能说明修复解决了真实存在的缺陷。为此另做一次
**反证**：临时写入一个绕过写锁、直接对裸 `*websocket.Conn` 并发写的测试用例（即修复前的写法），
在同样的 `-race` 条件下执行。

| 项 | 修复前写法（临时用例） | 修复后写法（`console.Conn`） |
|---|---|---|
| 写入目标 | 裸 `*websocket.Conn` | `Conn`（写锁串行化） |
| `-race` 结果 | **报告 6 条 `DATA RACE`，测试 FAIL** | 无告警，PASS |
| 结论 | 并发写确实触发数据竞争 | 写锁消除竞争 |

该临时用例仅用于取证，取证完成后已删除，不留在仓库中（避免仓库内长期存在一个必然失败的测试）。

## 5. 性能与对比实验

> 三组实验均为对运行环境的实测，数据如实记录、不作外推。每组均注明环境与测试命令口径；
> 涉及对照组的（OpenStack / Proxmox VE）引用的是官方文档口径，注明为量级对比而非同条件实测。

### 5.1 实验环境

| 项 | 值 |
|---|---|
| 磁盘 | NVMe SSD × 2（Great Wall GW3000 1TB + SK hynix 512GB，`lsblk` 报告 ROTA=0） |
| 虚拟化 | libvirt（`qemu:///system`，unix socket 直连） |
| 基准卷 | `base` 存储池 `rocky10.qcow2`（虚拟容量 10GiB / 实际数据 529MB，qcow2） |
| 计时口径 | 后端任务执行路径毫秒计时，不含 HTTP 往返与前端渲染 |

### 5.2 实验 1：linked clone 与全量拷贝

同一基准卷、同一存储池，对比两条建卷路径：

- **全量拷贝**：`StorageVolCreateXMLFrom`（等价 `virsh vol-clone`）——修复前「增量克隆」的真实实现路径；
- **linked clone**：`StorageVolCreateXML` + 卷 XML 声明 `<backingStore>`（等价 `qemu-img create -f qcow2 -F qcow2 -b 父盘 子盘`）——修复后路径（见 10.2 F-19）。

| 组 | 测试口径 | 轮次 | 各轮耗时（ms） | 均值 | 产物实际磁盘占用（`du`） | 产物 backing file |
|---|---|---|---|---|---|---|
| 全量拷贝 | `StorageVolCreateXMLFrom`（`virsh vol-clone`） | 3 | 970 / 964 / 965 | ≈966 | 1031MB | **无** |
| linked clone | `StorageVolCreateXML` + `<backingStore>` 卷 XML（`qemu-img create -b`） | 5 | 38 / 33 / 34 / 32 / 35 | ≈34 | ≈1MB | 指向源卷（qcow2） |

linked clone 产物验证（口径固定为三条外部命令，见 10.2 F-19）：

- `qemu-img info`：`backing file:` 指向源卷，且 `backing file format: qcow2`；
- `qemu-img check`：`No errors were found on the image`（镜像结构无错误）。

结论：

1. 本组数据下 linked clone **约 28 倍提速**、产物磁盘开销**千倍级缩减**（1031MB → ≈1MB）。
2. 比单点数字更重要的是两组的性质差异：全量拷贝的耗时随源卷**实际数据量**线性增长（本例源卷实际
   数据 529MB 即耗时约 1 秒，源盘越接近写满耗时等比放大），linked clone 只写元数据，耗时与数据量
   无关——即克隆耗时对用户是一个**可预期的常数**，而全量拷贝不可预期。这一性质差异决定了后者才配
   称「增量克隆」。
3. 方法论呼应（见 10.2 F-19）：「调用了哪个 API」不构成功能语义的证明——全量组正是当年「名不副实」
   的实现路径，其外部可观测结果（`qemu-img info` 无 `backing file` 行）就是反证；本实验同样以外部
   可观测结果（计时、`du`、`qemu-img info`）作为两组的唯一判据，不以代码注释为准。

### 5.3 实验 2：平台自身资源占用

| 项 | 占用 | 采样口径 |
|---|---|---|
| vmops 生产实例 | RSS 35.9MB / 16 线程 | `ps` 采样；宿主机约 30 个域定义 |
| 冷启动新实例 | RSS 24MB | 启动完成后即采样 |
| 平台二进制 | 27MB | 单文件，已内嵌前端产物 |
| MySQL（容器） | 204.8MB | `docker stats` |
| **核心合计（平台 + MySQL）** | **≈240MB** | 上述两项之和 |
| Prometheus（可选） | 31.4MB | 容器 |
| Alertmanager（可选） | 14.2MB | 容器 |
| Grafana（可选） | 221.7MB | 容器 |

对照组（官方文档口径，量级对比）：

- OpenStack 安装指南最低要求：controller 4GB + compute 2GB（all-in-one 部署的实践建议 16GB+）；
- Proxmox VE 官方最低要求：2GB（OS + 管理服务，不含 guest）。

结论：vmops 控制面自身内存占用比上述两家低 **1–2 个数量级**（≈240MB 对 2–4GB 量级），监控栈为可选
组件且单组件不超过 222MB。受限硬件（如 8GB 内存的实验室旧机器）即可承载平台本体与若干小规格 guest，
这正是「轻量级私有云管理平台」定位的资源侧依据。

### 5.4 实验 3：部署耗时

| 项 | 实测 / 口径 |
|---|---|
| 冷启动 | **206ms**（进程启动到 API 可响应，含 MySQL 连接、GORM AutoMigrate、种子数据检查；后端启动日志时间戳） |
| 部署动作 | 拷贝 27MB 单二进制 + 一个可用的 MySQL（compose 一键拉起） |
| 对照 | OpenStack 手工部署的公开实践通常以小时计（组件多、依赖关系复杂；此处为量级对比而非同条件实测） |

结论：单二进制 + 单数据库的部署形态把「平台可用」收敛为一次文件拷贝，冷启动亚秒级；与多组件分布式
控制面的部署复杂度差距在一个数量级以上。

## 6. 安全验证记录

本轮安全加固共 8 组验证，全部为对运行中服务的实测（`SERVER_MODE=debug`，本机 `:8080`），
其中 SSH 目标白名单一组的证据取自服务端日志的拒绝记录。

### 6.1 SQL 注入面（路径参数主键）

改造后所有 `:id` 先经 `paramID` 解析为 `uint` 才进入 GORM，非法值在进库前即被拒绝。

| 编号 | 输入（`GET /api/vms/<id>`） | 预期 | 实际 |
|---|---|---|---|
| S-SQL-01 | `abc` | 400 `ID 参数非法` | `{"code":400,"message":"ID 参数非法"}` ✓ |
| S-SQL-02 | `1 OR 1=1` | 400 `ID 参数非法` | 同上 ✓ |
| S-SQL-03 | `1;DROP` | 400 `ID 参数非法` | 同上 ✓ |
| S-SQL-04 | `1' AND '1'='1` | 400 `ID 参数非法` | 同上 ✓ |
| S-SQL-05 | `1 UNION SELECT 1` | 400 `ID 参数非法` | 同上 ✓ |
| S-SQL-06（对照） | `0` | 400（`parseID` 拒绝 0） | `{"code":400,"message":"ID 参数非法"}` ✓ |
| S-SQL-07（对照） | `99999`（合法但不存在） | 404 `虚拟机不存在`（行为不变） | `{"code":404,"message":"虚拟机不存在"}` ✓ |

### 6.2 RBAC：只读角色与客户机写入通道

| 编号 | 输入 | 预期 | 实际 |
|---|---|---|---|
| S-RBAC-01 | viewer `GET /api/vms/1/terminal` | 403 + 中文原因 | `{"code":403,"data":null,"message":"只读角色不能使用 SSH 终端与串口控制台，请使用图形控制台查看"}` ✓ |
| S-RBAC-02 | viewer `GET /api/vms/1/serial` | 403 + 同上文案 | 同上 ✓ |
| S-RBAC-03 | viewer `GET /api/vms`（对照） | 200 | 200 ✓ |
| S-RBAC-04 | viewer `POST /api/vms/1/start` | 403 `需要管理员权限` | ✓ |
| S-RBAC-05 | viewer `GET /api/users` | 403 `需要管理员权限` | ✓ |
| S-RBAC-06 | admin `GET /api/vms/1/terminal`（非 WS 升级请求） | 通过中间件，由 WebSocket 升级器拒绝（400） | 400（证明中间件已放行 admin） ✓ |

### 6.3 图形控制台只读标记

| 编号 | 输入 | 预期 | 实际 |
|---|---|---|---|
| S-VO-01 | admin 对运行中 VM 请求 `POST /api/vms/:id/vnc-token` | `data.view_only == false` | ✓ |
| S-VO-02 | viewer 对同一 VM 请求同接口 | `data.view_only == true` | ✓ |
| S-VO-03 | 前端按 `view_only` 拼 noVNC URL | 追加 `&view_only=1`，键鼠输入禁用，顶栏显示只读标签 | ✓ |

### 6.4 JWT 算法混淆

用 python3 手工构造 token（payload 均为 `{user_id:1, username:"admin", role:"admin"}`，未过期）：

| 编号 | 输入 | 预期 | 实际 |
|---|---|---|---|
| S-JWT-01 | header `{"alg":"none"}`，签名段留空 | 401 | `{"code":401,"data":null,"message":"Token 无效或已过期"}` ✓ |
| S-JWT-02 | header `{"alg":"HS512"}`，用内置默认密钥以 HMAC-SHA512 签名 | 401 | 同上 ✓ |
| S-JWT-03（对照） | header `{"alg":"HS256"}`，同一密钥签名 | 200，返回 VM 列表 | 200 ✓ |

对照组成立说明两点：一是测试环境确实在使用内置默认密钥（因此 release 模式必须拒绝该密钥），
二是 S-JWT-01/02 的 401 确由 `WithValidMethods` 的算法限定产生，而非密钥或时间戳原因。

### 6.5 XML 注入面（libvirt 定义）

| 编号 | 输入 | 预期 | 实际 |
|---|---|---|---|
| S-XML-01 | `POST /api/storage/pools`，`path` = `/tmp/x</path></target><target><path>/etc` | 400 | `{"code":400,"message":"存储池路径必须是规范的绝对路径（不含 ..、结尾斜杠），且不能是根目录"}` ✓ |
| S-XML-02 | 同接口，`path` = `/tmp/../etc` | 400 同上文案 | ✓ |
| S-XML-03 | 同接口，`path` = `/tmp/x/`（结尾斜杠） | 400 同上文案 | ✓ |
| S-XML-04 | 同接口，`path` = `/`（根目录） | 400 同上文案 | ✓ |
| S-XML-05 | `POST /api/storage/pools/vmops/volumes`，`format` = `qcow2"/><target><path>/etc/x</path></target><x y="` | 400 `卷格式只支持 qcow2 或 raw` | ✓ |
| S-XML-06 | 同接口，`format` = `vmdk`（白名单外的合法格式名） | 400 同上文案 | ✓ |
| S-XML-07 | `POST /api/networks`，`gateway` = `192.168.99.1"><forward mode="bridge"/><x z="` | 400 `网关必须是合法的 IPv4 地址` | ✓ |
| S-XML-08 | 同接口，`gateway` = `999.1.1.1` | 400 同上文案 | ✓ |

接口层拦截之外，virt 层已改为 `encoding/xml` 序列化，即便取值域校验有遗漏也无法改变 XML 结构（纵深防御）。

### 6.6 命令行选项注入（宿主机 `ssh_ip`）

该值会作为 argv 传给 `exec.Command("ping", ..., host.SSHIP)`。

| 编号 | 输入（`POST /api/hosts` 的 `ssh_ip`） | 预期 | 实际 |
|---|---|---|---|
| S-ARG-01 | `-f`（ping 的 flood 选项） | 400 `SSH 地址格式不合法，只能是 IP 或主机名` | ✓ |
| S-ARG-02 | `h;id` | 400 同上文案 | ✓ |
| S-ARG-03 | `-c 100000` | 400 同上文案 | ✓ |
| S-ARG-04（对照） | `192.168.1.10` | 200 创建成功 | ✓（测试后已删除该记录） |
| S-ARG-05（对照） | `kvm-node1.local` | 200 创建成功 | ✓（测试后已删除该记录） |

### 6.7 Web 终端 SSH 目标白名单

对未记录 IP 的 VM（`vms.ip` 为空）经 WebSocket 首帧提交不同目标，服务端拒绝日志如下（`vm=Docker(10)`）：

| 编号 | 输入 target | 预期 | 实际（服务端日志 `reason=`） |
|---|---|---|---|
| S-SSH-01 | `1.1.1.1:22`（公网） | 拒绝 | `只允许连接私有网段地址（10/8、172.16/12、192.168/16）` ✓ |
| S-SSH-02 | `8.8.8.8:22`（公网） | 拒绝 | 同上 ✓ |
| S-SSH-03 | `127.0.0.1:22`（环回，可 SSH 进宿主机自身） | 拒绝 | `该地址不允许作为终端目标（环回 / 链路本地 / 组播）` ✓ |
| S-SSH-04 | `169.254.169.254:80`（云元数据地址） | 拒绝 | 同上 ✓ |
| S-SSH-05 | `example.com:22`（主机名） | 拒绝 | `目标必须是 IP 地址（不支持主机名）` ✓ |
| S-SSH-06 | `192.168.122.50:22`（私有网段） | 放行至真实拨号 | 放行，`SSH 拨号失败 err=dial tcp ... i/o timeout` ✓ |
| S-SSH-07 | `10.0.0.5:22`（私有网段） | 放行至真实拨号 | 放行，`SSH 拨号失败 err=dial tcp ... no route to host` ✓ |
| S-SSH-08 | `192.168.122.50:99999`（端口越界） | 拒绝 | `端口不合法（须在 1-65535 之间）` ✓ |

S-SSH-06/07 的「拨号失败」是预期结果：校验只负责放行到拨号阶段，目标不可达属网络层面的正常失败，
恰好证明校验放行的是真实连接尝试而非提前短路。

### 6.8 中间件响应格式统一

`middleware/jwt.go` 原返回 `{"error": "..."}`，与全站契约 `{code, message, data}` 不一致。

| 编号 | 输入 | 预期 | 实际 |
|---|---|---|---|
| S-FMT-01 | 无 `Authorization` 头 | 401 `{code,message,data}` | `{"code":401,"data":null,"message":"未提供认证信息"}` ✓ |
| S-FMT-02 | `Authorization: Token xxx` | 401 | `{"code":401,"data":null,"message":"认证格式错误"}` ✓ |
| S-FMT-03 | `Authorization: Bearer abc.def.ghi` | 401 | `{"code":401,"data":null,"message":"Token 无效或已过期"}` ✓ |
| S-FMT-04 | viewer 发起写操作 | 403 | `{"code":403,"data":null,"message":"需要管理员权限"}` ✓ |
| S-FMT-05（不得变更） | `GET /api/vnc/token/bad`（websockify 回调，外部契约） | 404 `{"error":...}` 保持原样 | `{"error":"token 无效或已过期"}` ✓ |

S-FMT-05 是有意保留的例外：该接口由 websockify 的 JSONTokenApi 插件消费，响应格式由 websockify
规定，改为统一格式会使 noVNC 链路整体失效。

### 6.9 登录限流（确认项，功能行为见 2.18）

2.18 已记录功能行为（同 IP 1 分钟内失败 5 次即锁定、第 6 次返回 429「登录失败次数过多」、成功登录
清零计数、不同 IP 互不影响）。此处从安全视角补充确认三点实现语义（`handler/auth.go` 的
`loginLimiter`，附单元测试）：

- 锁定期间**正确密码同样拒绝**——不向爆破者泄露「密码已经猜中」这一信号；
- 计数为进程内存实现、按来源 IP 维度（1 分钟窗口 / 5 次失败阈值），重启即清零——取舍是不为此引入
  额外存储依赖，锁定粒度以单实例为界；
- 超限响应与其他错误一样走统一中文文案，不回显内部细节。

### 6.10 /metrics 抓取令牌

`/metrics` 面向 Prometheus 抓取，原先公开无鉴权。现支持经环境变量 `METRICS_TOKEN` 开启 Bearer
认证（Prometheus 抓取任务以 `bearer_token` 配对配置；令牌也可经 `?token=` 传递）：

| 编号 | 场景 | 预期 | 实际 |
|---|---|---|---|
| S-METRICS-01 | 未设置 `METRICS_TOKEN`，`GET /metrics` | 200，Prometheus 可直接抓取（兼容默认形态） | ✓ |
| S-METRICS-02 | 已设置令牌，不带 Authorization 头 | 401 | ✓ |
| S-METRICS-03 | 已设置令牌，`Authorization: Bearer <错误值>` | 401 | ✓ |
| S-METRICS-04 | 已设置令牌，携带正确 Bearer（Prometheus 侧配对 `bearer_token`） | 200，抓取恢复 | ✓ |

说明：令牌经环境变量注入，不落代码与仓库；未设置时端点保持公开（启动日志会提示开启令牌或以防火墙
限制 `:8080` 的来源网段），是否开启由部署方按网络边界自行决定。

### 6.11 CORS release 校验

`CORS_ORIGINS=*` 意味着任意站点可携带凭据跨域调用全部 API。release 模式下启动即校验并拒绝：

| 编号 | 场景 | 预期 | 实际 |
|---|---|---|---|
| S-CORS-01 | `SERVER_MODE=release` 且 `CORS_ORIGINS` 为 `*` 或未配置 | 启动即 `log.Fatalf` 拒绝，中文日志给出配置示例 | ✓（`main.go` 启动校验） |
| S-CORS-02 | `SERVER_MODE=release` 且配置了具体来源 | 正常启动，仅该来源可跨域 | ✓ |
| S-CORS-03（对照） | `SERVER_MODE=debug`（开发默认） | 保持 `*`，不触发校验 | ✓（开发形态不受影响） |

## 7. 稳定性与容错验证

| 编号 | 用例 | 输入/操作 | 预期 | 实际 |
|---|---|---|---|---|
| R-RUN-01 | `go run main.go` 可启动 | 仓库根目录执行 `go run main.go`（二进制位于 `/tmp/go-build*/`） | 正常启动并托管前端产物 | 启动成功，日志 `前端产物目录: /home/jiuzhao/vmops/web/dist`，`Listening and serving HTTP` ✓ |
| R-RUN-02 | 产物缺失不退出进程 | 四候选目录均无 `index.html` | 降级为「仅 API」，`/` 返回 503 中文提示，进程存活 | 代码路径已覆盖（`locateWebRoot` 返回 `nil` 分支），日志给出构建指引 ✓ |
| R-AUD-01 | 静态资源不写审计 | 依次请求 `/assets/index-*.js`、`/favicon.ico`、`/vms`（SPA 路由回退） | `audit_logs` 不新增记录 | 三次请求后审计总数仅因 `/api/audit` 自身查询 +1，静态与 SPA 请求 +0 ✓ |
| R-TASK-01 | 入队超时可观测 | 队列满且持续 3 秒无空位 | 任务置 `failed`，`error` 为「任务队列繁忙，请稍后重试」，前端首次轮询即得终态 | 代码路径已覆盖（`enqueue` 超时分支写日志 + `markFailed` + 回写返回 task） |
| R-TASK-02 | executor panic 不拖垮进程 | executor 内发生 panic | 任务置 `failed`，`error` 为「任务执行异常，请查看服务日志」，堆栈只进日志，worker 继续取下一个任务 | 代码路径已覆盖（`runExecutor` + `run` 两层 recover） |
| R-WS-01 | 并发写不 panic | 见 4 章 U-Conn-01 | 无 panic，`-race` 无告警 | PASS ✓ |
| R-WS-02 | 强制断开可收敛 | 见 4 章 U-Conn-04/05 | 对端收到带中文原因的关闭帧，读循环退出 | PASS ✓ |

说明：R-TASK-01/02 与 R-RUN-02 属于难以在真实环境稳定复现的分支（需人为制造队列积压、注入 panic、
删除构建产物），本轮以代码路径审查与日志分支确认为准，未做破坏性注入实验，此处如实标注。

## 8. 静态检查与构建验证

| 项 | 命令 | 结果 |
|---|---|---|
| 编译 | `go build ./...` | 通过，无输出 |
| 静态分析 | `go vet ./...` | 通过，无输出 |
| 格式 | `gofmt -l .` | 无待格式化文件 |
| 单元测试 | `go test -race ./...` | 138 个顶层函数 / 约 950 子用例 / 7 个包，全 PASS（纯函数目标覆盖率基本 100%） |
| E2E 回归 | `./scripts/smoke.sh` | `RESULT: PASS=23 FAIL=0` |
| 容器构建 | `docker build --no-cache -t vmops:verify .` | **成功，74 秒，产物 51.5MB**（Dockerfile 重写后验证，详见 docs/07 §3） |
| 深度 lint | `golangci-lint run ./...`（`.golangci.yml` 已配置 govet/errcheck/staticcheck/unused/ineffassign/gofmt/revive，`go:` 已对齐 `1.25`） | **未执行（本机未安装该工具），列为待补项** |

> 关于深度 lint 的两点如实说明：① 本机确实没有安装 `golangci-lint`，因此该项不能计入「静态检查全绿」；
> ② 现有 `.golangci.yml` 为 v1 schema，若安装 v2.x 版本会因字段改名（`linters-settings` → `linters.settings` 等）
> 直接报配置错误，接入时需先迁移配置（已在 docs/07 §8 记录）。

## 9. 真实 KVM 演示验证


- [x] 通过平台创建多台 VM（vmops 池建盘，virsh vol-list 对照）。
- [x] 与 virsh list --all / dumpxml 结果对照（UUID/MAC/磁盘路径/状态）。
- [x] 增量克隆三条命令验收：子盘 `qemu-img info` 有 `backing file:` 行、`virsh vol-dumpxml` 有 `<backingStore>` 节点、`qemu-img check` 报 `No errors were found on the image`（这三条固定为该功能的验收方式，见 docs/05 §6.7.1）。
- [x] 子卷与父卷占用对比（`virsh vol-info` / `qemu-img info` 对照写时复制效果：虚拟容量相同，实际占用差距显著）。
- [x] 克隆机与源机 MAC/UUID 对照（`virsh dumpxml` 与 DB 记录双向核对）。
- [x] 删源机后 backing chain 完好性验证（`kept_volumes` 给出保留原因 + 克隆机磁盘 `qemu-img check` 无错误）。
- [x] 截图与录屏待答辩前补（论文插图）。

## 10. 测试结果与分析

- [x] 接口测试用例表（编号/输入/预期/实际/结论）：见第 3 章（功能 15 条）、第 4 章（单元 13 条）、第 6 章（安全 52 条）、第 7 章（稳定性 7 条），合计 87 条编号用例。
- [x] 审计数据分布图表素材（DB 现有 7500+ 条，覆盖 9 种操作）。
- [x] 问题与修复记录：
  - 存储池卷名含扩展名被拒 → 放开卷名校验（允许 `.`）。
  - 删除运行中 VM 域残留 → UndefineDomain 先 Destroy 再 UndefineFlags。
  - 有快照的 VM 删除失败 → UndefineFlags 带 SNAPSHOTS_METADATA。
  - ConnectListAllDomains NeedResults=0 返回空 → 传 1。
  - 同步 Stop 15s 撞 axios 超时 → 转异步 `stop_vm` 任务 + 前端轮询。
  - VNC 会话幽灵残留 → token 解析刷新 + 60 分钟过期清扫收敛。

### 10.1 问题与修复记录（P0 稳定性 + P1 安全）

| 编号 | 问题 | 触发条件 / 后果 | 修复 | 验证 |
|---|---|---|---|---|
| F-01 | 多协程并发写同一 WebSocket | SSH 桥 3 路写、串口桥 2 路写、强制断开 1 路写；所用库禁止并发写，违反即 panic，且 panic 在协程内部，Web 框架 Recovery 拦不住，整进程退出 | 新增 `console.Conn`，写锁串行化所有写入 | U-Conn-01～05 + 反证实验（修复前写法 `-race` 报 6 条 DATA RACE） |
| F-02 | 全仓库 `recover()` 出现 0 次 | worker / 清扫器 / WS 转发协程任一 panic 即终止进程 | 三处任务侧 + 清扫器 + 3 个 WS 转发协程共 7 处兜底 | R-TASK-02、代码路径审查 |
| F-03 | 队列满时另起协程阻塞入队 | 协程随提交频率无上限堆积且永不退出 | 改 `enqueue` 有界等待（`enqueueTimeout=3s`），超时置 failed + 中文原因 | R-TASK-01 |
| F-04 | `go run main.go` 必然启动失败 | 静态托管只看可执行文件所在目录，而 `go run` 的二进制在 `/tmp/go-build*/`，随即 `log.Fatalf` 退出；而 README 与 AGENTS.md 都把 `go run main.go` 写作标准运行方式 | `locateWebRoot` 四候选探测（含当前工作目录），全未命中降级为「仅 API」不退出 | R-RUN-01/02 |
| F-05 | 路径参数未解析直传 GORM（39 处） | ORM 把非数字字符串当原始 SQL 片段拼进 WHERE；只读角色可达的查询接口即可布尔盲注读 `users.password_hash` | 新增 `paramID`/`parseID`，全部先解析为 `uint` | S-SQL-01～07 |
| F-06 | libvirt XML 字符串拼接（5 处） | 池 name/path、卷 name/format、网络 name/gateway 可闭合标签注入任意定义 | 全部改 `encoding/xml` 结构体序列化 + 接口层白名单 | S-XML-01～08 |
| F-07 | JWT 未限定签名算法 | 可改写 `alg` 走不同验签路径（算法混淆） | `ParseToken` 加 `WithValidMethods(["HS256"])` | S-JWT-01～03 |
| F-08 | release 密钥校验是死代码 | 只判 `== ""`，而 config 兜了硬编码默认值使条件永不成立；默认值随源码公开，可伪造 admin token | 改为「空或等于内置默认值」即拒绝启动 | S-JWT-03 反向印证（默认密钥确可签出有效 token） |
| F-09 | Web 终端 SSH 目标零校验 | host/port/user/password 全取自浏览器；平台成为跳板机、内网端口扫描器、口令爆破器 | `validateSSHTarget` 私有网段白名单 + 端口范围 + 拒主机名 | S-SSH-01～08 |
| F-10 | 宿主机 `ssh_ip` 零校验 | 该值作为 argv 传给 `ping`，`-f` 被当成 flood ping 选项 | `validateSSHIP`（IP 或保守主机名白名单） | S-ARG-01～05 |
| F-11 | 只读角色可经控制台写客户机 | `/terminal`、`/serial` 是 GET，按方法放行即绕过 RBAC；串口在多数云镜像上直接是 root TTY | `isGuestWriteChannel` 对 viewer 403；图形控制台改下发 `view_only` 并在客户端禁用键鼠 | S-RBAC-01～06、S-VO-01～03 |
| F-12 | 中间件响应格式与全站契约不一致 | 返回 `{"error":...}`，前端统一解包逻辑失效 | 包内 `abortJSON` 输出 `{code,message,data}`（不能复用 handler 的 `Fail`，否则构成导入环） | S-FMT-01～05 |
| F-13 | 审计中间件读全量请求体后弃用 | 多 GB 镜像上传时把请求体读进内存，可致 OOM | 删除该死代码，并显式设置 `MaxMultipartMemory = 32MB`（超出落磁盘） | R-AUD-01（同批修复的白名单） |
| F-14 | 静态资源与 SPA 回退淹没审计表 | 开一次页面即一批 `/assets` 请求 | 白名单增加 `/assets`、`/favicon.ico`，并跳过 SPA 路由回退 | R-AUD-01 |
| F-15 | 上下文类型断言无 `ok` | `role.(string)`、`user_id.(uint)` 类型不符即 panic 掉请求链 | 全部改为带 `ok` 的断言并降级处理 | 代码路径审查 |
| F-16 | 终端读循环裸 `break` | 只跳出 `switch` 而非外层 `for`，stdin 写失败时循环不退出 | 改为带 `readLoop` 标签的 `break` | 代码路径审查 |
| F-17 | 种子口令写入启动日志 | 日志常被收集/共享，默认口令随之外泄 | 只打用户名与角色，口令改指向 README | 启动日志核对 |
| F-18 | 清扫器持锁做数据库 IO | 锁持有时间与查询往返次数成正比，阻塞全部会话开启与关闭 | 锁内取快照 → 锁外查库 → 回锁删除并二次确认 | 代码路径审查 |

### 10.2 问题与修复记录（P2 正确性）

本批与 P0/P1 的性质不同：P0 修的是「会让进程死掉」的稳定性缺陷，P1 修的是「会被外部攻击者利用」的
安全缺陷，P2 修的是**「功能表面可用、实际语义与文档声称不符」的正确性缺陷**。这类缺陷不报错、不崩溃，
只在特定操作序列下造成不可恢复的数据损坏，因此只能靠外部工具复核功能语义才能发现。

| 编号 | 问题 | 触发条件 / 后果 | 修复 | 验证 |
|---|---|---|---|---|
| F-19 | **「增量克隆」并未实现，而 README 与多篇文档都把它作为核心特性宣称** | 原 `CloneVolumeFromVol` 用 `StorageVolCreateXMLFrom`（等价 `virsh vol-clone`），函数注释还写着「libvirt 自动写 qcow2 backing file」；实测该 API 是**全量数据拷贝**，产出的子卷没有 backing file。功能退化为整盘复制，但因为拷贝也能正常开机，缺陷长期未被察觉 | 改用 `StorageVolCreateXML` + 卷 XML 内声明 `<backingStore>`（等价 `qemu-img create -f qcow2 -F qcow2 -b`）；新增 `buildVolumeXMLWithBacking` 与 `volBackingXML`，`buildVolumeXML` 退化为薄封装 | T-Clone-01（三条外部命令：`qemu-img info` 有 `backing file:`、`vol-dumpxml` 有 `<backingStore>`、`qemu-img check` 无错误；修复前前两条均不存在） |
| F-20 | 克隆虚拟机的 MAC 与源机完全相同 | `CloneVMFromSpec` 用 `spec := *source` 派生，`Disks` 做了深拷贝但 **`Interfaces` 没有**；libvirt 会拒绝重复 UUID、但**不校验 MAC**，XML 里显式给了就照用。故无任何报错，直到两台同时开机才出现同网段 ARP 冲突、网络双双不可用 | 新增 `randomMACAddr()`（KVM 保留前缀 `52:54:00`）；抽出纯函数 `buildCloneSpec`（深拷贝 Disks 与 Interfaces、重生成 UUID、**逐块网卡换 MAC**、清空 RawXML）与 `systemDiskIndex`（跳过 cdrom）；并更正 `execCloneVM` 里「MAC 从 libvirt 查询（克隆后重新生成）」的错误注释 | U-Clone-01～08（尤其 U-Clone-04）、T-Clone-02 |
| F-21 | 删除虚拟机会误删共享卷 | `execDeleteVM` 原先只按「是否在存储池路径下」判断，会删掉：① 增量克隆父盘（真正实现 linked clone 后风险成立）→ backing chain 断裂，全部子机磁盘不可读且不可恢复；② 镜像库登记的共享基镜像（「基于云镜像创建」走 `source_image_id` 是直接引用不拷贝）→ 文件被删而 `images` 表记录仍在，留下悬挂记录 | 新增 `shouldKeepVol` 三重守卫（池外文件 / 镜像库登记 / 仍被子卷当 backing file），命中即保留并写中文原因；配套新增 virt 层 `ListBackingRefs(pool)` 枚举「父卷路径 → 子卷名列表」；保留的卷进任务结果 `kept_volumes` 数组并打日志 `[tasks] 保留卷（未删）` | T-Clone-03、T-Clone-04（3 组用例全 PASS） |
| F-22 | Dockerfile 无法构建 | ① `golang:1.21` 不满足 `go.mod` 的 `go 1.25.0`；② `go build -o vmops ./...` 必报 `cannot write multiple packages to non-directory`（仓库有 11 个包）；③ 容器内 `proxy.golang.org` 超时使 `go mod download` 挂死；④ alpine 无 `/usr/share/zoneinfo` 而 DSN 带 `loc=Local`，时间静默退化 UTC 差 8 小时；⑤ `alpine:3.19` 已 EOL；⑥ `web/dist/` 被 gitignore，干净克隆里 `COPY web/dist` 直接失败 | 整文件重写：`golang:1.25-alpine`、`-o vmops .`、`ARG GOPROXY=https://goproxy.cn,direct`、`-tags timetzdata` + `ENV TZ=Asia/Shanghai`、`alpine:3.24`、`web/dist` 改从 builder 阶段拷贝并 `mkdir -p` 兜底 | `docker build --no-cache` 成功，74 秒，产物 51.5MB（见第 7 章） |
| F-23 | `vms.status` 列默认值与状态映射不一致 | `model/vm.go` 的 gorm 默认值写作 `shut_off`（下划线），而 `StateToPlatform` 返回 `shut off`（空格），前端亦按空格版本匹配；一旦某行落到列默认值，其状态在前端无法匹配任何已知状态 | 默认值改 `shut off`；新增 `model.VMStatus*` 与 `virt.Status*` 两套常量，`handler/vm.go` 8 处字面量改引用（virt 刻意不 import model，避免把 GORM 拖进封装层） | 数据库核查确认**无** `shut_off` 存量行（所有写入路径都显式给值）；沙箱验证 GORM `AutoMigrate` 能改已有列默认值，重启即收敛 |
| F-24 | `ImportVMs` 内部错误泄漏（handler 层最后一处） | `fmt.Sprintf("%s: %v", name, err)` 把 GORM 原始错误（表结构、约束名）拼进响应 `errors` 数组 | 完整错误经 `LogError` 进日志，响应只给「域名 + 写入数据库失败」 | 代码路径审查；同时更正一处认知：该 `errors` 数组前端 `VmList.vue` 实际未消费，失败原因暂不呈现给用户（列入 10.3） |
| F-25 | `.golangci.yml` 的目标语言版本落后 | `go: "1.21"` 与 `go.mod` 的 `go 1.25.0` 不一致，linter 会按旧版本语言规则检查，产生误报/漏报 | 改为 `go: "1.25"` | 无法执行验证（本机未安装 golangci-lint，见第 7 章） |

关于 F-19 的方法论意义（论文可展开）：修复前的代码调用了名字看起来完全正确的 libvirt API，注释、
README、设计文档三方自洽，唯独与 qemu-img 的实际输出不符。这说明**「调用了哪个 API」不构成功能语义
的证明**，必须由该功能的外部可观测结果反证。此后「增量克隆」的验收被固定为三条外部命令
（见 docs/05 §6.7.1），不再以代码注释为准。

### 10.3 尚未处理的已知项（不作已解决表述）

| 项 | 现状 | 可达角色 / 影响 |
|---|---|---|
| `POST /api/networks/xml`、`PUT /api/networks/:name`、`PUT /api/vms/:id/xml` | 接受调用方原始 XML 直接定义，无结构校验 | 仅 admin |
| `GET /metrics` | 未设置 `METRICS_TOKEN` 时公开（启动日志有提示）；设置后要求 Bearer 认证（见 6.10）。生产环境建议开启令牌或以防火墙限制来源网段 | 任意来源（未开启令牌时） |
| Web 终端 SSH 主机密钥 | `InsecureIgnoreHostKey()`，残余中间人风险（目标已限私有网段） | 仅 admin |
| `golangci-lint` | 本机未安装，深度 lint 未执行；配置为 v1 schema，装 v2.x 需先迁移字段名 | 静态检查覆盖不完整（`go build`/`go vet`/`gofmt` 已过） |
| 状态字面量 | `service/tasks/vm_tasks.go` 仍有 5 处 `"shut off"` 字面量未换成常量 | 一致性隐患，当前行为正确 |
| 导入失败原因 | `POST /api/vms/import` 的 `errors` 数组前端 `VmList.vue` 未消费（只读三个计数） | 单台导入失败时用户看不到具体原因 |
| 跨宿主机操作 | 多宿主机空壳已砍除（`hosts.libvirt_uri` 字段及入口已移除），宿主机模块重新定位为「登记与状态采集」（本机 `qemu:///system` 真实可管） | 跨宿主机虚拟化操作（`qemu+ssh://` 等）仍为后续工作（见 docs/08） |

> 曾列于此的「孤儿卷无自动清理入口」已闭环：`POST /api/storage/pools/:name/orphan-cleanup` 转
> `cleanup_volumes` 后台任务，复用删卷守卫的引用判定反向使用（零引用才删），验证见 2.17。

## 11. 性能测试（可选）

- [x] KVM 操作耗时：linked clone vs 全量拷贝的对比计时已实测（见第 5 章实验 1），任务进度上报可作为其余操作的耗时分布素材。
- [ ] 接口响应时间统计（列表/详情等只读接口的 P50/P95，待补）。

---

## 📌 素材出处

- 旧《开发进度》测试记录与问题记录
- DB audit_logs（现有 7500+ 条审计数据）
- 实测：virsh list --all（15 台已定义 VM）
- `go test -race ./...` 输出（138 个顶层测试函数 / 约 950 个子用例 / 7 个包，全 PASS；早期批次为 `service/virt` 8/8 + `service/console` 5/5）
- `qemu-img info` / `qemu-img check` / `virsh vol-dumpxml` / `virsh dumpxml` 输出（增量克隆 backing file、克隆机 MAC、删机后共享卷保留）
- `docker build --no-cache` 输出（74 秒，产物 51.5MB）
- 服务端日志 `vmops.log`（SSH 目标拒绝记录、前端产物目录探测、任务与会话收敛、`[tasks] 保留卷（未删）`）
- curl / python3 / wscat 构造的安全验证载荷与响应
- 性能对比实验实测数据：两种建卷路径的毫秒计时（3 + 5 轮）、`du` 产物占用、`qemu-img info`/`check` 产物验证、`ps`/`docker stats` 资源采样、后端启动日志时间戳（冷启动 206ms）

